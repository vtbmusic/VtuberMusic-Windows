<UserControl
    x:Class="VtuberMusic_UWP.Components.DataItem.AlbumCardItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VtuberMusic_UWP.Components.DataItem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:components="using:VtuberMusic_UWP.Components"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{ThemeResource SystemControlAcrylicElementBrush}" CornerRadius="10" Margin="5 14" Shadow="{StaticResource Shadow}" Translation="0,0,32">
        <StackPanel>
            <Grid x:Name="CoverImgBorder" Height="150" Width="280" CornerRadius="10">
                <Image Source="{x:Bind Source.coverImgUrl, Mode=OneWay}" Stretch="UniformToFill"/>
            </Grid>
            <StackPanel Height="50" Padding="10 5">
                <TextBlock Foreground="{ThemeResource SystemBaseMediumLowColor}" TextTrimming="CharacterEllipsis" FontSize="12" Text="{x:Bind Source.creator.nickname, Mode=OneWay}"/>
                <components:MarqueeText Text="{x:Bind Source.name, Mode=OneWay}" FontSize="16"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
