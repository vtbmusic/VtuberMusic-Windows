<UserControl
    x:Class="VtuberMusic_UWP.Components.Main.TopPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VtuberMusic_UWP.Components.Main"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:account="using:VtuberMusic_UWP.Components.Account"
    xmlns:player="using:VtuberMusic_UWP.Components.Player"
    mc:Ignorable="d" Background="{x:Null}"
    d:DesignHeight="1080"
    d:DesignWidth="1920">

    <Grid>
        <StackPanel Orientation="Horizontal" Padding="25 0 0 20">
            <Button x:Name="GoBackButton" Style="{StaticResource CircleButton}" Height="40" Width="40" FontSize="18"
                    FontFamily="{StaticResource Icon}" Content="{StaticResource icon-back}" IsEnabled="False"/>
            <Button x:Name="GoForwardButton" Style="{StaticResource CircleButton}" Height="40" Width="40" FontSize="18"
                    FontFamily="{StaticResource Icon}" Content="{StaticResource icon-go}" Margin="5 0 3 0" IsEnabled="False"/>
            <TextBox x:Name="SearchKeyword" CornerRadius="18" KeyDown="TextBox_KeyDown" FontSize="14" Height="40" Padding="13 8 0 0" VerticalAlignment="Center" BorderBrush="Transparent" PlaceholderText="搜索" Width="400"/>
        </StackPanel>
        <PersonPicture Height="35" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="40 0">
            <PersonPicture.ProfilePicture>
                <BitmapImage UriSource="https://cdn.aqua.chat/user/1298272546046611456a60613b465f2dd5c6f5848d3feb40ffd.jpg" DecodePixelHeight="40" DecodePixelWidth="40"/>
            </PersonPicture.ProfilePicture>
        </PersonPicture>
    </Grid>
</UserControl>
