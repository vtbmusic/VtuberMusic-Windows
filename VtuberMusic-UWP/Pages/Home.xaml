<Page
    x:Class="VtuberMusic_UWP.Pages.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VtuberMusic_UWP.Pages"
    xmlns:main="using:VtuberMusic_UWP.Components.Main"
    xmlns:models="using:VtuberMusic_UWP.Models.VtuberMusic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:collections="using:VtuberMusic_UWP.Components.Collections"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="BannerItemTemplate" x:DataType="models:Banner">
            <Grid>
                <Image Source="{x:Bind BannerImg}" Stretch="UniformToFill"/>
                <Border VerticalAlignment="Bottom">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#00000000"/>
                            <GradientStop Color="#90000000" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="{x:Bind OriginName}" Margin="10"
                               Foreground="White" FontSize="24" FontWeight="Bold"/>
                </Border>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel MaxWidth="1100" Margin="100 0">
            <main:BlockTitle Text="我们的推荐"/>
            <FlipView x:Name="BannerDataView" Margin="0 5" Height="530" ItemTemplate="{StaticResource BannerItemTemplate}"/>
            <Grid Margin="0 30" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel MinWidth="400">
                    <main:BlockTitle Text="我关注的"/>
                    <collections:MusicDataList x:Name="SubMusicDataView" Mode="Large"/>
                </StackPanel>
                <StackPanel Grid.Column="1" MinWidth="400" Margin="5 0">
                    <main:BlockTitle Text="热门播放"/>
                    <collections:MusicDataList x:Name="HotMusicDataView" Mode="Large"/>
                </StackPanel>
                <StackPanel Grid.Column="2" MinWidth="400">
                    <main:BlockTitle Text="最新歌曲"/>
                    <collections:MusicDataList x:Name="NewMusicDataView" Mode="Large"/>
                </StackPanel>
            </Grid>
            <main:BlockTitle Text="人气 Vtuber"/>
            <GridView x:Name="VtuberDataView">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:Artist">
                        <Grid Padding="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <PersonPicture ProfilePicture="{x:Bind imgUrl}" Margin="5"/>
                            <TextBlock Grid.Row="1" Text="{x:Bind name.origin}" HorizontalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            <main:BlockTitle Text="歌单"/>
            <GridView x:Name="AlbumDataView" SelectionChanged="AlbumDataView_SelectionChanged">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:Album">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="150"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Image Source="{x:Bind coverImgUrl}" Width="280" Stretch="UniformToFill"/>
                            <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="5" Text="{x:Bind name}"/>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </ScrollViewer>
</Page>
