<Page
    x:Class="VtuberMusic_UWP.Pages.Album"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VtuberMusic_UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:collections="using:VtuberMusic_UWP.Components.Collections"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="30">
            <Grid animations:Connected.AnchorElement="{x:Bind CoverImg}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*" MaxWidth="465"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel CornerRadius="10">
                    <Image x:Name="CoverImg" animations:Connected.Key="Cover" Stretch="UniformToFill">
                        <Image.Shadow>
                            <ThemeShadow/>
                        </Image.Shadow>
                    </Image>
                </StackPanel>
                <Grid Grid.Column="1" Margin="20 15 0 0">
                    <StackPanel>
                        <TextBlock x:Name="AlbumName" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock x:Name="CreatorInfo" Text="Misaka-L 创建于 2021/5/23" FontSize="24" FontWeight="Bold"/>
                        <TextBlock x:Name="Introduction" Margin="0 10" Text="这是一个简介"/>
                    </StackPanel>
                    <Button Content="播放全部" Margin="0 0 0 5" VerticalAlignment="Bottom"/>
                </Grid>
            </Grid>
            <collections:MusicDataList x:Name="DataView" Margin="0 30 0 0" Mode="Large"/>
        </StackPanel>
    </ScrollViewer>
</Page>
